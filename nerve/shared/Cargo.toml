[package]
name = "nerve-shared"
version = "0.1.0"
edition = "2021"
description = "Gemeinsamer Code fuer AIUX Nerves (MQTT, Config-Typen)"

[dependencies]
rumqttc = "0.24"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
toml = "0.8"
